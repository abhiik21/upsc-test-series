# v68 — One page for Test Series and Pricing

- **Plans & pricing now live on the Test Series page** (`test-series.html#plans`), below the series cards: the three plans with prices and access period read from the database (a plan switched off in the admin disappears), plus a "Plans & Pricing" button in the page header and two FAQ answers (one-time payment, what happens after paying).
- The separate **Pricing page is gone from the menus**. `pricing.html` remains only as a forwarding page to `test-series.html#plans`, so old links and bookmarks still work.
- Every link that pointed to Pricing (footers, buttons, "Unlock with a plan", checkout, blocked-test redirect) now goes to the plans section.
- Not carried over from the old Pricing page: the plan comparison table, the "What you get" block and the pricing FAQ entries that duplicated the series FAQ. The plan cards' text is unchanged, including the bullet points that still do not match what each plan really unlocks (see v66 notes).
